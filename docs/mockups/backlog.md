# Mockup Backlog

Issues and improvements for the Pipeline Explorer mockup gallery. Each item has enough context for a coder to resolve independently.

## Status Key

| Symbol | Meaning |
|--------|---------|
| `[ ]` | Open |
| `[X]` | Resolved |

---

## Open Items

| Status | ID | Mockup | Description |
|--------|----|--------|-------------|
| `[ ]` | BL-01 | (missing) | **Add Build & Deploy mockup (`08_build_deploy.html`)**. The "deploy" summary group (accent `#795548`) has 5 nodes in the `deployment` lane: `proc_build_static` (Static Site Builder), `proc_fetch_shim` (Fetch Shim Injection), `proc_deploy` (Deploy to GitHub Pages), `proc_health` (Health Check), `proc_cli` (CLI Commands). Internal edges: `build_static → fetch_shim → deploy` (sequential chain), `cli → download_mgr` (run-all), `cli → ts_metrics` (timeseries-refresh), `cli → health` (status). Ghost incoming arrows from all 12 interface nodes (they feed into `build_static`). Show CLI commands as a separate orchestration node with conditional edges labeled "run-all", "timeseries-refresh", "status". Add to `index.html` sidebar as item 8. Update `mockup_plan.md` with a new MK-08 row. |
| `[ ]` | BL-02 | `01_data_sources.html` | **"BLS sources only" label is hidden behind the Browser Header Workaround node**. The condition label `rect` at (510, 400) with text "BLS sources only" overlaps with or is obscured by the Browser Header Workaround node at translate(470, 430). Fix: move the condition label higher (y ~380) or to the right side of the edge, so it sits clearly between the Download Manager and Browser Header nodes, not behind/under the target node. |
| `[ ]` | BL-03 | `00_summary_topology.html` | **Spacing between blocks is not uniform**. Blocks have irregular horizontal and vertical gaps. Fix: establish a consistent grid — e.g., 180px horizontal gap center-to-center on the main spine, 160px vertical gap for branched rows. Re-position all 8 blocks so spacing is visually even. Pay special attention to the gap between Validation Gates and Core Warehouse (currently compressed by the "gates pass" label) and the gap between Marts & Web and Build & Deploy (currently too tight because the blocks have different widths). |
| `[ ]` | BL-04 | `00_summary_topology.html` | **Time-Series should be visually part of Core Warehouse, not a separate top-level block**. In the summary topology, Time-Series is a side path off Core Warehouse, not an independent pipeline stage. Fix: remove Time-Series as a standalone block. Instead, show it as a sub-block or annotation within/attached to the Core Warehouse block (e.g., a smaller attached box below Core Warehouse labeled "Time-Series Enrichment" with a loop arrow). Update the topology arrows accordingly — the arrow from Core to Marts should show that time-series enrichment happens as part of the Core→Marts flow. This also means updating `index.html` sidebar (remove or merge the Time-Series entry) and potentially merging `06_time_series.html` content into `05_core_warehouse.html`. |
| `[ ]` | BL-05 | `01_data_sources.html` | **SOC Crosswalk should be a required data source, not optional**. Currently the hexagon has a dashed border (`stroke-dasharray="4,3"`) and `stroke="#64748b"` (gray) instead of `#3b82f6` (blue). The arrow from crosswalk to Download Manager uses optional styling. Fix in mockup: change the hexagon stroke to `#3b82f6`, remove `stroke-dasharray`, change text fill to `#f1f5f9` (white), change the arrow from dashed `#64748b` to solid `#94a3b8` with the standard `url(#arrow)` marker. **Also fix in code**: in `pipeline_graph_data.js`, change the edge `{ from: "src_crosswalk", to: "proc_download_mgr", type: EDGE_TYPES.OPTIONAL }` to `type: EDGE_TYPES.REQUIRED`. Similarly change `{ from: "proc_download_mgr", to: "store_raw_xwalk", type: EDGE_TYPES.OPTIONAL }` to `REQUIRED`, and `{ from: "store_raw_xwalk", to: "proc_parse_xwalk", type: EDGE_TYPES.OPTIONAL }` to `REQUIRED`, and `{ from: "proc_parse_xwalk", to: "gate_schema_drift", type: EDGE_TYPES.OPTIONAL }` to `REQUIRED`, and `{ from: "gate_soc_alignment", to: "store_crosswalk", type: EDGE_TYPES.OPTIONAL }` to `REQUIRED`. |
| `[ ]` | BL-06 | `02_raw_landing.html` | **Overlapping objects**. The 6 cylinder nodes are stacked too tightly — Raw CPI Files (y=382) and Raw Crosswalk Files (y=432) overlap or nearly touch. Fix: increase vertical spacing between all nodes. Current y-offsets are 82, 157, 232, 307, 382, 432 — the last gap is only 50px while others are 75px. Use consistent 80px spacing: 82, 162, 242, 322, 402, 482. Also increase the SVG height to 620 if needed. Ensure ghost arrows on both sides are re-aligned to match new node centers. |
| `[ ]` | BL-07 | `02_raw_landing.html` | **Raw Crosswalk Files should be required, not optional**. Currently the cylinder has dashed stroke (`stroke-dasharray="4,3"`) and gray color (`#64748b`). Fix: change stroke to `#e91e63` (matching other raw nodes), remove `stroke-dasharray`, change text fill to `#f1f5f9`. This is consistent with BL-05 making crosswalk a required data source. |
| `[ ]` | BL-08 | `03_stage_parse.html` | **Crosswalk Parser should be required, not optional**. Currently the node at translate(190, 515) has dashed stroke and gray color. Fix: change stroke to `#4caf50` (matching other parser nodes), remove `stroke-dasharray="4,3"`, change text fills from `#94a3b8`/`#64748b` to `#f1f5f9`/`#94a3b8`/`#4caf50` (matching other parser nodes). Consistent with BL-05. |
| `[ ]` | BL-09 | `04_validation_gates.html` | **Items partially obscured by other items**. The diamond gate nodes and their connecting edges overlap in several places: (1) The Schema Drift node at translate(105, 90) has a duplicate `<g>` element — there are two overlapping diamond groups (one with `style="display:none"` but the other at translate(105, 90) draws on top). Remove the first orphaned `<g>` block. (2) The blocked-path Bézier curve from Schema Drift to Grain passes through/behind the Referential Integrity diamond. Fix: adjust the curve control point to route below all three top-row diamonds (e.g., `Q 165 280, 480 280` to drop the curve lower). (3) The "drift blocks publication" label at (240, 230) may overlap with the Referential diamond. Move it further down (y ~265) or route it more cleanly under the diamonds. (4) The SOC Alignment output edge and "SOC loads first" label path at y=365 runs very close to the bottom row of diamonds. Verify all 6 diamonds and all edge labels are fully visible with no clipping. |
| `[ ]` | BL-10 | `05_core_warehouse.html` | **SOC Crosswalk Mappings should be required, not optional**. Currently the node at translate(445, 470) has dashed stroke and gray color. Fix: change stroke to `#1976d2` (or `#42a5f5` if categorized as a fact-like table), remove `stroke-dasharray="4,3"`, change text fills to match other core nodes. Consistent with BL-05. |
| `[ ]` | BL-11 | `05_core_warehouse.html` | **Arrows and lines are not accurate**. The FK constraint edges only show `dim_occupation → fact_wages`, `dim_occupation → fact_proj`, `dim_occupation → bridges`, and `dim_geography → fact_wages`. Missing edges from the actual `GRAPH_EDGES` data: (1) `dim_occupation → mart_summary` should not be here (that's a Core→Mart edge, shown as ghost). (2) The ghost outgoing arrows go to generic "Time-Series" and "Marts" labels but don't accurately represent which specific core nodes feed which downstream nodes. Fix: add the missing internal FK line `dim_geography → fact_wages` (already present, verify visible), add `store_dim_cpi → proc_ts_cpi_deflation` as a ghost outgoing arrow (labeled "CPI → Time-Series deflation"), add `store_crosswalk → proc_ts_comparable` as a ghost outgoing arrow (labeled "Crosswalk → comparable history"). Review all edges in `GRAPH_EDGES` that have both `from` and `to` nodes in the `core` lane and ensure each one is drawn. |
| `[ ]` | BL-12 | `05_core_warehouse.html` + `06_time_series.html` | **Time-Series Enrichment should be part of the Core Warehouse diagram**. Time-Series is not a separate pipeline stage — it's an enrichment process within the warehouse layer. Fix: merge the 6 time-series nodes from `06_time_series.html` into `05_core_warehouse.html`. Add a second section/sub-lane within the Core Warehouse mockup labeled "Time-Series Enrichment" (using the `#9c27b0` accent). Show the 6 time-series process nodes below the existing 8 core storage nodes, with edges connecting `fact_wages → proc_ts_observations`, `dim_occupation → proc_ts_metrics`, `dim_cpi → proc_ts_cpi_deflation`, `crosswalk → proc_ts_comparable`. The combined diagram will have 14 nodes. Either remove `06_time_series.html` or convert it to a redirect/note pointing to the merged diagram. Update `index.html` sidebar accordingly. This is the diagram-level fix for BL-04. |
| `[ ]` | BL-13 | `07_marts_web.html` | **API & Web Pages should be split into two separate sections**. Currently all 12 interface nodes are in a single "API & Web Pages" lane. Fix: split into two sub-lanes within the mockup: (1) **API Endpoints** — nodes that serve data: search API (`/api/occupations/search`), hierarchy API, occupation API, wages API, trends API, CPI API, health check API. (2) **Web Pages** — nodes that serve HTML: Search Page, Hierarchy Browser, Occupation Profile, Wage Comparison, Trend Explorer, Occupation Compare, Geography Compare, Ranked Movers, CPI Explorer, Methodology, Lessons, Pipeline Explorer. Show arrows from API nodes to their corresponding Web Page nodes where applicable. This better represents the architecture where web pages consume API endpoints. Adjust the SVG width/height to accommodate the three-column layout (Marts → APIs → Pages). |
| `[ ]` | BL-14 | `07_marts_web.html` | **Arrows and lines are incorrect**. Several edge issues: (1) The `dim_cpi → iface_cpi` edge is drawn as a ghost from "Core" but `dim_cpi` feeds the CPI Explorer directly — this should be an explicit incoming arrow labeled "dim_cpi (Core)". (2) The `store_mart_similarity → iface_occupation` optional edge arrow points upward to y=240 but visually crosses over multiple other edges making it hard to follow. Route it more cleanly (e.g., via the right side). (3) Methodology and Lessons interface nodes have no incoming mart arrows in the mockup because they don't consume marts directly — they should still show ghost incoming arrows from their actual data sources or be marked as "content-only" nodes. (4) All 12 interface→build_static ghost arrows converge at a single point (1080, 300) creating a visual mess. Fan them out or use a collector bar/funnel shape. (5) Missing edge: `store_fact_proj` (Core) feeds into occupation profile indirectly through marts — verify the full edge chain is represented or annotated. |

| `[ ]` | BL-15 | all except `00_summary_topology.html` | **Missing hover effect on nodes**. The summary topology has CSS hover effects on `.block` elements (brightness increase, border thickening) that make the diagram feel interactive. Mockups 01–07 have no hover effects on their node `<g>` elements. Fix: add a `.node` class to every node `<g>` element in mockups 01–07, and add the same CSS hover rule used in `00_summary_topology.html`: `.node { cursor: pointer; transition: filter 0.2s; } .node:hover { filter: brightness(1.15); }`. Apply consistently to all node types (hexagons, rounded rects, cylinders, diamonds, pills). This makes the gallery feel cohesive — every diagram should respond to mouse interaction the same way. |

---

## Resolved Items

(none yet)
